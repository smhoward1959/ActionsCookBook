# A first workflow to play around with the editor
name: My First Workflow
on: 
  push:
    branches:
      - main
jobs:
  first_job:
    name: My first job
    runs-on: ubuntu-latest
    steps:
      - run echo "Hello world from ${{github.actor}}."
      - name: Checkout
        uses: actions/checkoput@v5.0.0
        - run: tree
    
    
